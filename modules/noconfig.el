;; Packages that don't have any special configuration. Just install
;; them and off we go.

(setup (:package ag
                 auto-complete
                 dash
                 dockerfile-mode
                 go-mode
                 json-mode
                 json-reformat
                 kubernetes
                 scratch
                 terraform-mode
                 wrap-region
                 yaml-mode))
